.user-icon.participant
  .initial-avatar= user.initials
  .avatar-text= user.full_name

  - if defined?(status)
    .status.d-inline-flex.flex-column.align-items-center
      .status-text.d-inline-flex
        %em= status == 'creator' ? 'creator' : 'invited'