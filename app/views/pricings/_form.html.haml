-# frozen_string_literal: true
= simple_form_for(@pricing) do |f|
  .card-body
    = f.input :title, wrapper_html: { class: 'mb-3' }
    = f.input :description, wrapper_html: { class: 'mb-3' }
    = f.input :price, wrapper_html: { class: 'mb-3' }

  .card-footer.d-flex
    %button.btn.btn-primary{ type: 'submit' }
      Save
    = link_to 'Back', pricings_path, class: 'btn btn-link ms-auto'
