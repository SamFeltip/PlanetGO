#where-timetable.py-1
  - if @outing.proposed_events.empty?
    %p
      You haven't added any events to your outing yet.
  - else
    - @outing.proposed_events.each do |proposed_event|
      = render 'events/event_card', event: proposed_event.event, render_params: { add_event_to_outing: false, in_event: true }, proposed_event: proposed_event
